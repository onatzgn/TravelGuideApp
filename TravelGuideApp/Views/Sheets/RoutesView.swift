import SwiftUI

struct RoutesView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("Routes View")
            }
            .navigationBarTitleDisplayMode(.inline)
        }
    }
}
//KALDIR
